<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat Client" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="UsernameTextBox" Width="120" Margin="0,0,5,0" Text="Username"/>
            <TextBox x:Name="IpAddressTextBox" Width="120" Margin="0,0,5,0" Text="127.0.0.1"/>
            <TextBox x:Name="PortTextBox" Width="60" Margin="0,0,5,0" Text="8888"/>
            <Button x:Name="ConnectButton" Content="Connect" Width="80" Click="ConnectButton_Click"/>
        </StackPanel>

        <TextBox Grid.Row="1" x:Name="ChatArea" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="MessageInput" Margin="0,0,5,0"/>
            <Button Grid.Column="1" x:Name="SendButton" Content="Send" Width="80" Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>